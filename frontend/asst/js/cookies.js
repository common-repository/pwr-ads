var url_string = "http://127.0.0.1:8000/approval?utm_source=facebook&utm_medium=cpc&utm_term=google&utm_content=co&utm_campaign=page&adid=46&adtype=custom&token_key=3pSPLbyedT";// var url_string = window.location.href;// alert('test');var base_url = window.location.origin;var url = new URL(url_string);var token_key = url.searchParams.get("token_key");if (token_key != null) {    document.cookie = "Pwr-Ads=" + token_key;    function getCookie(name) {        // Split cookie string and get all individual name=value pairs in an array        var cookieArr = document.cookie.split(";");        // Loop through the array elements        for (var i = 0; i < cookieArr.length; i++) {            var cookiePair = cookieArr[i].split("=");            /* Removing whitespace at the beginning of the cookie nameand compare it with the given string */            if (name == cookiePair[0].trim()) {// Decode the cookie value and return                return decodeURIComponent(cookiePair[1]);            }        }        // Return null if not found        return null;    }}if (token_key != null) {    document.cookie = "Pwr-Ads=" + token_key;    $.ajax({ type: "get", url: "https://portal-pwr-ads.com/tracking/" + token_key, });}